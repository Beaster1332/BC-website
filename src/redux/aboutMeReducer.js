const initialState = {
    myInfo: '',
}

let aboutMeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default aboutMeReducer;